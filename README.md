# store-admin fixed v3

Ganti `index.html` dan `admin.js` di GitHub.

Jangan mengganti `firebase-config.js` jika konfigurasi Firebase di GitHub sudah benar.

Versi ini memuat Firebase secara lazy sehingga error konfigurasi tidak membuat tombol login mati.
