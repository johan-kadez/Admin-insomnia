# store-admin

Versi perbaikan tombol login.

Catatan:
- Jangan mengganti firebase-config.js dengan versi placeholder.
- Pastikan firebase-config.js di GitHub berisi konfigurasi Firebase project yang benar.
- Firebase Authentication > Sign-in method > Email/Password harus aktif.
- Login memakai email akun Firebase + password, bukan username.
